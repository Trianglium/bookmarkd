import { FilterByNetworkPipe } from './filter-by-network.pipe';

describe('FilterByNetworkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNetworkPipe();
    expect(pipe).toBeTruthy();
  });
});

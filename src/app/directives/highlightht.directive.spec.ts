import { HighlighthtDirective } from './highlightht.directive';

describe('HighlighthtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighthtDirective();
    expect(directive).toBeTruthy();
  });
});
